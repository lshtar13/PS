#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
typedef unsigned long long ull;
#define CORD(x) (x+500000)
int n, hor[1000001] = {0}, ver[1000001] = {0};

int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    cin>>n;
    for(int x, y, _x, _y, i = 0; i<n; ++i)
    {
        cin>>_x>>_y;
        if(x == _x)
        {
            
        }
        else
        {

        }
        x = _x, y = _y;
    }

    return 0;
}