#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    int n, m;
    cin >> n >> m;
    char board[2500];

    for (int i = 0; i < n; ++i)
        for (int l = 0; l < m; ++l)
            cin >> board[i * m + l];

    int change[25], min = 0x7fffffff;

    for (int i = 1; i < 8; ++i)
    {
        2 * i - 1 ~ 2 * (i + 1) - 1
    }

    return 0;
}