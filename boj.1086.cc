#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

const int MAX_CASES = (1<<15);

int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int n, dp[][MAX_CASES];
    cin>>n;
    return 0;
}